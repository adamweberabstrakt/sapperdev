"use client";

// Placeholder — uncomment and configure when Drift is ready
// import Script from "next/script";

export default function Chatbot() {
  // Return null until Drift demo is complete and embed code is provided
  return null;

  // When ready, replace with:
  // return <Script src="https://js.driftt.com/include/YOUR_ID.js" strategy="lazyOnload" />;
}
